# Copy to  frontend/.env.production  and fill in your deployed API URL.
# Vite reads this automatically during `npm run build`.
# This value is baked into the built JS and is NOT a secret (it's just a URL).

VITE_API_BASE_URL="https://amr-api-39499444372.us-central1.run.app/api/v1"
